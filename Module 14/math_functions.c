#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    /*
      double x;
      scanf("%lf", &x);
      //* ceil()
      // int ans = ceil(x);
      //* floor()
      // int ans = floor(x);
      //* round()
      int ans = round(x);
    */

    //*  sqrt()
    /*
    int x;
    scanf("%d", &x);
    double ans = sqrt(x);
    printf("%lf", ans);
    */

    //* pow()
    /*
    int a, b;
     scanf("%d %d", &a, &b);
     int ans = pow(a, b);
     printf("%d", ans);
     */

    //* abs()
    int x;
    scanf("%d", &x);
    int ans = abs(x);
    printf("%d", ans);
    return 0;
}